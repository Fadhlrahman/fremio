# LOCAL STAGING (SAFE) - ONLY LOCALHOST
# Goal: frontend never hits production by default.

# Backend mode
VITE_BACKEND_MODE=vps

# Use same-origin /api and let Vite proxy it to backend
VITE_API_URL=/api
VITE_PROXY_TARGET=https://localhost:5050

# Optional legacy flags (some code may still read these)
VITE_USE_VPS=true
VITE_DISABLE_HTTPS=false

# App metadata
VITE_APP_NAME=Fremio (Local Staging)
VITE_APP_URL=http://localhost:5180

# Midtrans Payment Gateway (Frontend)
# Get client key from: https://dashboard.midtrans.com/
# Must match the backend MIDTRANS_CLIENT_KEY
VITE_MIDTRANS_CLIENT_KEY=Mid-client-J1httIljA1-8-Kh7
VITE_MIDTRANS_IS_PRODUCTION=false
