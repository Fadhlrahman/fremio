PORT=5050
NODE_ENV=development
FRONTEND_URL=http://localhost:5180

# Database (PostgreSQL)
# Preferred: use DB_* (used by current ESM server routes)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fremio
DB_USER=salwa
DB_PASSWORD=

# Optional legacy connection string
DATABASE_URL=postgresql://salwa@localhost:5432/fremio

# JWT Configuration
JWT_SECRET=fremio-dev-secret-key-2024-local-testing
JWT_EXPIRES_IN=7d

# Firebase Configuration (Optional)
# For local staging, keep this pointing to a NON-production key file (or missing file) to avoid accidents.
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.staging.json
FIREBASE_STORAGE_BUCKET=

# File Upload Limits
MAX_FILE_SIZE=10485760
MAX_IMAGE_SIZE=5242880
MAX_VIDEO_SIZE=52428800

# Temp File Cleanup
TEMP_FILE_CLEANUP_HOURS=24
