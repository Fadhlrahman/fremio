export const BACKGROUND_PHOTO_Z = -4000; // Background layer stays behind everything else
export const PHOTO_SLOT_MIN_Z = 0; // Photo slots minimum z-index (above background, can be customized by user)
export const NORMAL_ELEMENTS_MIN_Z = 1; // Normal elements (shapes, text, uploads) start here
export const CAPTURED_OVERLAY_Z_OFFSET = 0.001; // Captured overlays sit just above their placeholders for rendering
