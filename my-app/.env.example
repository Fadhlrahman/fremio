# ==============================================
# FREMIO FRONTEND ENVIRONMENT VARIABLES
# ==============================================

# Backend Mode (choose one)
# Set to 'true' to use VPS backend, 'false' or omit to use Firebase
VITE_USE_VPS=false

# VPS API URL (only needed when VITE_USE_VPS=true)
VITE_API_URL=https://api.yourdomain.com/api

# Firebase Configuration (default mode)
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# ImageKit (for image optimization - optional)
VITE_IMAGEKIT_PUBLIC_KEY=your-imagekit-public-key
VITE_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-id

# Midtrans Payment Gateway
VITE_MIDTRANS_CLIENT_KEY=your-midtrans-client-key
VITE_MIDTRANS_IS_PRODUCTION=false

# Backend API Base URL (for local dev or VPS)
VITE_API_BASE_URL=http://localhost:3000

# ==============================================
# NOTES:
# ==============================================
# 
# FIREBASE MODE (default):
# - Uses Firebase Auth for authentication
# - Uses Firestore for data storage
# - Uses localStorage as fallback for frames
# - No VPS backend needed
#
# VPS MODE:
# - Uses VPS backend API for all data
# - JWT authentication via VPS
# - PostgreSQL database on VPS
# - Requires deploying backend to VPS first
#
# To switch to VPS mode:
# 1. Deploy backend to VPS
# 2. Set VITE_USE_VPS=true
# 3. Set VITE_API_URL to your VPS API URL
# 4. Restart dev server
# ==============================================
